{
	"name": "ru-pass",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"publish": "npm run build && surge --domain https://ru-pass.surge.sh ./dist"
	},
	"devDependencies": {
		"vite": "^5.4.8",
		"vite-plugin-pwa": "^0.20.5"
	}
}
